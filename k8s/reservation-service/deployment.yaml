apiVersion: apps/v1
kind: Deployment
metadata:
  name: reservation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reservation-service
  template:
    metadata:
      labels:
        app: reservation-service
    spec:
      containers:
        - name: reservation-service
          image: lkassous/reservation-service:latest  # Remplace si nécessaire
          ports:
            - containerPort: 5003
          env:
            - name: DATABASE_URL
              value: postgresql://admin:admin@postgres:5432/reservationsdb
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
